# web-vuln-scanner
A defensive Python scanner that checks a website's on configuration for common security weaknesses (missng security headers, insecure cookies, exposed files. Educational/defensive use only.
